language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t axray/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true axray/docker-react npm run test -- --coverage